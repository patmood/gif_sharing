<div id="neighbors" data-prev="<%= @previous_token %>" data-token="<%= @image.token %>" data-vote="<%= @vote %>" data-next="<%= @next_token %>" ></div>
<div id="huge">
  <img src="<%= @image.url %>" >
</div>

<div id="upvote"><button class="btn vote"><i class="icon-arrow-up"></i></button></div>
<div class="score"><%= @image.score %></div>
<div id="downvote"><button class="btn vote"><i class="icon-arrow-down"></i></button></div>
<h1>HUGEGIF</h1>
